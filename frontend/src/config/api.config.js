const BASE_URL = "/api/v1"; // Using proxy

export const API = {
  BASE: BASE_URL,
  AUTH: `${BASE_URL}/auth`,
  HR: `${BASE_URL}/hr`,
  EMPLOYEE: `${BASE_URL}`,
  ADMIN: `${BASE_URL}/admin-dashboard`,
};
